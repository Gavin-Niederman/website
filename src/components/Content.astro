<div class="content-container">
    <div class="content">
        <slot/>
    </div>
</div>

<style lang="scss">
    @import "../styles/theme.scss";

    .content-container {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        z-index: 100;
    }

    .content {
        margin: 10px;
        padding: 30px;
        display: flex;
        max-width: 800px;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        border-radius: 30px;

        border-width: 10px;
        border-color: #1D201F;
        border: 5, solid, var(--col-bg) ;

        background: rgba(#1D201F, 0.6);
        backdrop-filter: blur(20px);
        filter:  drop-shadow(3px 3px 6px rgba(0, 0, 0, 0.468));
    }

    .content :global(p, li) {
        font-size: 1.5rem;
        margin-top: 0;
        margin: 0;
    }

    .content :global(a) {
        text-decoration: none;
		color: var(--col-orange)
    }
</style>